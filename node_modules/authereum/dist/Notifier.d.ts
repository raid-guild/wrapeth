declare type TNotifyType = 'success' | 'error' | 'pending';
interface INotifyOptions {
    timeout?: number;
    onClick?: () => void;
}
export default class Notifier {
    notifier: any;
    enabled: boolean;
    constructor(opts?: any);
    notify(kind: TNotifyType, message: string, options?: INotifyOptions): any;
    enable(enabled: boolean): void;
}
export {};
