import ProviderEngine from 'web3-provider-engine';
export default class AuthereumAdminKeyProvider extends ProviderEngine {
    authereum: any;
    rpcUri: string;
    isAuthereum: boolean;
    private config;
    private rpcSubprovider;
    private notifier;
    private adminPrivateKey;
    constructor(adminPrivateKey: string, networkUrl: string);
    private initSubprovider;
    enable(): Promise<void>;
    disable(): Promise<void>;
}
