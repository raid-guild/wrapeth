"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tld = 'authereum.com';
exports.defaultConfig = {
    apiUri(network) {
        const s = network === 'mainnet' ? '' : `${network}.`;
        return `https://${s}api.${tld}`;
    },
    rpcUri(network) {
        return `https://${network}.rpc.${tld}`;
    },
    webUri(network) {
        const s = network === 'mainnet' ? '' : `${network}.`;
        return `https://${s}${tld}`;
    },
    xsUri(network) {
        const s = network === 'mainnet' ? '' : `${network}.`;
        return `https://${s}x.${tld}`;
    },
    networkId(network) {
        return {
            'mainnet': 1,
            'ropsten': 3,
            'rinkeby': 4,
            'kovan': 42,
            'goerli': 5
        }[network] || 1;
    }
};
exports.blocknativeDappId = 'fccf560b-943a-45e9-9af3-6b19e44e167e';
