import { Emitter } from './interfaces';
declare function account(clientIndex: number, address: string): {
    emitter: Emitter;
    details: {
        address: string;
    };
};
export default account;
