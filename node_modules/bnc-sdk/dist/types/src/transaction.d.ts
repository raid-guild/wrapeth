import { Emitter, TransactionLog } from './interfaces';
declare function transaction(clientIndex: number, hash: string, id: undefined | string): {
    details: TransactionLog;
    emitter: Emitter;
};
export default transaction;
